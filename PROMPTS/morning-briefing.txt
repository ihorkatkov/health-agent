MORNING BRIEFING. Execute all sections:

1. SLEEP & HEALTH
Read latest health data from workspace/health/data/ (find most recent health-*.json).
Extract: sleep (totalSleep, deep, rem, core, awake, sleepStart, sleepEnd),
resting_heart_rate, heart_rate_variability, blood_oxygen_saturation,
respiratory_rate, apple_sleeping_wrist_temperature.
Load baselines from workspace/health/baselines.json.
Compare each metric against its baseline threshold.

2. CALENDAR
Check today's events across all configured calendars.
List the schedule with times.

3. RECOVERY SCORE
Based on sleep duration + HRV + resting HR, rate recovery:
🟢 Good — train hard, full cognitive load ok
🟡 Moderate — light training, manage energy
🔴 Poor — rest day, reduce load where possible

4. WORKOUT SUGGESTION
Based on recovery score and calendar gaps:
- Suggest type (strength / cardio / mobility / rest)
- Suggest time window that fits between calendar events
- Suggest duration

5. LOAD CHECK
If calendar is overloaded AND recovery is 🔴 or 🟡:
Name ONE thing to defer or cancel. Be specific.

Format: direct, personal, no filler. Be a coach, not a dashboard.
